<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.montyskew.momentum">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme"
        android:label="@string/app_name">
        <activity
            android:name="com.montyskew.momentum.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <fragment
            android:name="com.montyskew.momentum.Progress"
            android:label="@string/activity_progress"/>
        <fragment
            android:name="com.montyskew.momentum.Activities"
            android:label="@string/activities" />
        <fragment
            android:name="com.montyskew.momentum.Goal"
            android:label="@string/goal" />
        <fragment
            android:name="com.montyskew.momentum.Users"
            android:label="@string/users" />
    </application>

</manifest>
